# Notebook-ready workflow notes

This folder is intentionally lightweight. The Python workflows in `python/gradient_descent_ml/` and `calculators/python/` can be imported into notebooks for teaching, visualization, and extension.

Suggested notebook cells:

1. Load `gradient_descent_training_trace.csv`.
2. Plot loss by training step.
3. Compare learning-rate sensitivity.
4. Review optimization governance scores.
5. Discuss how loss reduction differs from responsible deployment readiness.

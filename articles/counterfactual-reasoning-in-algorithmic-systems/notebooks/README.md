# Notebook Notes

This folder is reserved for notebook versions of the counterfactual reasoning workflow. The scripts in `python/` are intentionally dependency-light so they can be run in ordinary terminal environments first, then adapted into notebooks for teaching or visualization.

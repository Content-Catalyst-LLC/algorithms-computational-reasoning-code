platform_layer(search).
platform_layer(cloud).
platform_layer(marketplace).
platform_layer(identity).
platform_layer(payments).
platform_layer(ai_platform).
platform_layer(analytics).

requires_governance(access_control).
requires_governance(ranking_audit).
requires_governance(api_changelog).
requires_governance(data_export).
requires_governance(appeal_mechanism).
requires_governance(portability_plan).
requires_governance(cost_visibility).

risk(lock_in).
risk(visibility_dependence).
risk(api_deprecation).
risk(measurement_asymmetry).
risk(opaque_suspension).
risk(no_appeal_path).
risk(provider_outage_dependency).

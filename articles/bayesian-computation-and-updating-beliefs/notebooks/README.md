# Notebook-ready workflow

Run the Python workflow first, then import the generated CSV files from `outputs/tables/` into a notebook environment for posterior diagnostics, prior sensitivity, sequential updating, and decision-threshold discussion.

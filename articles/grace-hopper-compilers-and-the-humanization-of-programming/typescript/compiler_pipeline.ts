const source = "ADD PAYROLL-TOTAL TO TAX-BASE";
const tokens = source.split(/\s+/);
console.log(`source=${source}`);
console.log(`tokens=${JSON.stringify(tokens)}`);
console.log("target_code=machine-specific instruction sequence");

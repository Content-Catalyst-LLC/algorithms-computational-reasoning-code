export function adjacencyMatrixUnits(vertices: number): number { return vertices * vertices; }
export function adjacencyListUnits(vertices: number, edges: number): number { return vertices + edges; }
export function totalSpace(input: number, output: number, auxiliary: number): number { return input + output + auxiliary; }

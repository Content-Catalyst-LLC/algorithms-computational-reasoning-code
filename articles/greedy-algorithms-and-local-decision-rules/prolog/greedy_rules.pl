greedy_pattern(local_choice).
greedy_pattern(feasibility_check).
greedy_pattern(commitment).
greedy_pattern(stopping_condition).
proof_need(greedy_choice_property).
proof_need(exchange_argument).
risk(proxy_optimization).
risk(premature_commitment).

# Article Notes

Core claim: debugging is computational reasoning. It compares expected behavior with observed behavior, uses traces and tests as evidence, forms and tests hypotheses, verifies fixes, preserves regression cases, and documents what the system revealed through failure.

# Notebook-ready workflow

Run the Python audit first:

```bash
python3 python/decision_classification/audit.py
```

Then import generated CSV and JSON outputs into a notebook for visualization or teaching.

allowed_transition(draft, review).
allowed_transition(review, published).
allowed_transition(review, draft).
allowed_transition(published, archived).
state_quality('Functional core with imperative shell', 89.04).

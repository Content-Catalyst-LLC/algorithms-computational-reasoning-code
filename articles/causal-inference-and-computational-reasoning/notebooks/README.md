# Notebooks

This folder is reserved for notebook versions of the Python and R workflows. The canonical runnable workflow is dependency-light and stored in `python/causal_inference_audit_workflow.py` so that the repository remains easy to run from the command line.

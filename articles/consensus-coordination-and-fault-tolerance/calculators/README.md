# Calculator Layer

Self-contained calculators for majority quorums, crash fault tolerance, majority availability, and Byzantine replica thresholds.

Run:

```bash
bash run_calculator_smoke_tests.sh
```

# Notebook Notes

This folder is reserved for notebook-ready versions of the representation-learning audit. The core workflow is dependency-light Python so it can run without notebook infrastructure.

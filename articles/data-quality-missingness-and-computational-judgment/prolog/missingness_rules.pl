missingness_reason(unknown).
missingness_reason(not_applicable).
missingness_reason(not_collected).
missingness_reason(withheld).
missingness_reason(pending).
missingness_reason(invalid).
missingness_reason(redacted).
missingness_reason(structural_zero).

quality_dimension(completeness).
quality_dimension(validity).
quality_dimension(freshness).
quality_dimension(provenance).
quality_dimension(representativeness).
quality_dimension(uncertainty_communication).

risk(treating_missing_as_zero).
risk(silent_row_dropping).
risk(imputation_without_flags).
risk(schema_drift).
risk(undercoverage).

# Binding, Scope, and Substitution Notes

Key ideas:

- a bound variable is controlled by a lambda abstraction;
- a free variable depends on context;
- scope defines where a binding applies;
- substitution must avoid variable capture;
- alpha conversion safely renames bound variables;
- closures in programming languages preserve function plus environment.

Formal computation depends on preserving meaning during symbolic transformation.

structure_reasoning_quality('Task scheduling priority queue', 82.32).
structure_reasoning_quality('Relationship analysis graph', 82.36).
structure_reasoning_quality('Case records table', 82.60).
structure_reasoning_quality('Document similarity vector index', 79.80).

supports(stack, backtracking).
supports(queue, ordered_service).
supports(heap, priority_retrieval).
supports(graph, relationship_traversal).
supports(table, relational_query).
supports(vector, similarity_search).

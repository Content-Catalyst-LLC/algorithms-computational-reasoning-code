sequence_structure_quality('Numerical time series array', 83.20).
sequence_structure_quality('Undo action stack', 83.80).
sequence_structure_quality('Case review queue', 85.04).
sequence_structure_quality('Streaming circular buffer', 83.80).

supports(array, indexed_access).
supports(list, sequential_traversal).
supports(stack, last_in_first_out).
supports(queue, first_in_first_out).
supports(deque, both_ends).
supports(circular_buffer, bounded_recent_history).

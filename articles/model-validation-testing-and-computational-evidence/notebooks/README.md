# Notebooks

Notebook-ready scaffolds for validation evidence should import generated tables from `outputs/tables/` rather than duplicate the analysis logic. Keep exploratory notebooks separate from the reproducible reference workflow in `python/` and `r/`.
